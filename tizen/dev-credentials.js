/*
 * Sign-in details for this development build, so the TV opens straight on the
 * content instead of on a form and an on-screen keyboard. The repository is
 * private; if it is ever made public again these have to move to repository
 * secrets (the workflow already writes this file from them) and be replaced
 * with the provider.
 *
 * Both portals belong to the same account: the first that answers wins.
 */
window.TalohimDev = {
  servers: ['http://ilvips.com:80', 'http://ilvip.net:80'],
  user: 'R62hJa6ZFX',
  pass: 'nyGefYq7PA'
};
